# 2 add in spatial stuff to show them how much faster python is than R -- introduce them to geopandas and have them make a map overlaid on contextily tiles and also make a map from a dataset using just `plot()`. so generate a fake spatial dataset

# do this in march for the lab + kathleen and logan